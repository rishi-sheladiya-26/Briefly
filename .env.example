# Supabase Database Configuration
# Copy this file to .env and fill in your actual values

DB_HOST=your-project-id.supabase.co
DB_USER=postgres
DB_PASSWORD=your_supabase_password
DB_PORT=5432
DB_NAME=postgres

# Complete database URL for easier connection (using session pooler)
# Replace with your actual connection string
DATABASE_URL=postgresql://postgres.your-project-id:your_password@aws-region.pooler.supabase.com:5432/postgres?sslmode=require

# Enable cloud database connection
USE_CLOUD_DB=True

# Django Secret Key (generate a new one for production)
SECRET_KEY=your-secret-key-here

# How to get these values:
# 1. Go to your Supabase dashboard: https://app.supabase.com
# 2. Create a new project or select existing project
# 3. Go to Settings > Database
# 4. Copy the connection parameters
# 5. For DATABASE_URL, use the "Session pooler" connection string
# 6. Replace the placeholders above with your actual values
